source "https://rubygems.org"

# jekyll
gem "jekyll", ">= 4.3", "< 5"
gem "webrick", "~> 1.7"

# ruby 3.3 自带 bigdecimal 3.x（无需编译）；固定版本避免解析到需要源码编译的 4.x
gem "bigdecimal", "~> 3.1"

# plugins
group :jekyll_plugins do
  gem "jekyll-sitemap"
  gem "jekyll-redirect-from"
  gem "jekyll-feed"
end
